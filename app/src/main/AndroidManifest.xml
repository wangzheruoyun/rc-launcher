<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".RcApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.RCLauncher">
        <!--
            Task 9 (screen-orientation adaptation):
              * screenOrientation="user"   - rotation is allowed and the device
                rotation lock is honoured; the launcher overrides this at runtime
                with sensorLandscape / sensorPortrait when the user forces an
                orientation (see MainActivity.applyOrientation).
              * configChanges=...          - the Activity handles every geometry
                change itself, so a rotation re-measures the Compose tree instead
                of recreating the Activity: no screen state is lost and the
                game / AWT surface keeps its coordinate space (which is what
                prevents touch coordinates from drifting after a rotation).
              * resizeableActivity="true"  - split screen / freeform windows are
                measured the same way as a rotation (WindowSizeClass driven).
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="user"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|uiMode|keyboardHidden|keyboard|navigation|touchscreen|fontScale|locale|layoutDirection"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

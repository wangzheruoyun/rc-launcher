1. [项目脚手架与多模块工程搭建] —— [采用 Gradle 多模块（:app 负责 Compose UI、:core 负责 Rust/JNI 桥接与运行时资源、:runtime 管理 JRE/库）与 Cargo workspace（Rust 核心库，经 cargo-ndk 交叉编译为 Android .so）；参照 FCL 的 FCL/FCLCore/FCLauncher/Terracotta/ZipFileSystem 模块边界与 cuberite 的 CMake 模块化思路，确立清晰的依赖方向与可测试单元。] [已完善 — 引入 gradle/libs.versions.toml 版本目录作为单一事实来源，根与各模块 build.gradle.kts 改用 catalog 别名（插件与依赖）；新增 Compose BOM（platform(libs.compose.bom)）+ resolutionStrategy.force 双保险，从目录统一锁定 compose-ui 1.5.4 / material3 1.2.0，根除 logcat/25_08-13-50-16_522.log 中因 Material3 调用 TextStyle.copy$default（Compose UI 1.6.x 合成方法签名）与运行时 compose-ui 1.5.4 不匹配导致的 java.lang.NoSuchMethodError 启动崩溃，确立不可漂移的依赖方向；并将 NDK ABI 收敛为仅 arm64-v8a（现代 Android 64 位要求），同步更新 :app/:core 的 abiFilters、CI rust-core 矩阵与 docs/BUILD.md、README 的 ABI 说明。]
2. [Rust 核心：异步下载管理器（断点续传 + 校验）] —— [基于 tokio + reqwest 实现并发分块下载器，支持 Range 续传（本地临时文件断点恢复）、并行分片、SHA-1/MD5 校验、失败指数退避重试与整体进度回调；吸收 FCLCore/download 与 cuberite 的健壮实现经验。] [已完成 — 实现位于 rust/crates/rc-launcher-core/src/download/{manager,client,hash,testing}.rs（基于 tokio + reqwest 的并发分块下载器：预分配 .part 临时文件 + 并行分片、Range 续传经 .part.meta 侧车文件做本地断点恢复、SHA-1/MD5 校验、指数退避重试、单一累计进度回调、download_all_concurrent 有界并发批下载）。健壮性在 FCLCore/download 与 cuberite 经验基础上进一步增强：① 当 .part 临时文件缺失/被截断时清空陈旧的已完成 chunks 集合，避免信任半成品导致最终校验失败（resets_stale_meta_when_temp_truncated）；② 当服务器忽略 Range 头返回整文件时（n > 期望长度）返回 RangeUnsupported 并自动回退到单流顺序下载而非失败（falls_back_to_single_shot_when_range_ignored_with_known_size）；③ 每任务多镜像 URL 回退：DownloadTask 新增 mirrors/with_mirror/urls()，分片与单流路径在失败时按序切换镜像重试，resolve() 探测尺寸时也遍历所有候选 URL（mirror_fallback_succeeds_when_primary_fails / mirror_fallback_succeeds_for_single_shot）；④ HTTP 429 处理：ReqwestSource 解析 Retry-After 头并返回 RateLimited{retry_after}，下载器重试时尊崇该建议退避（honors_rate_limited_retry_after + parses_retry_after_delta_seconds）。cargo test --workspace 全绿（download 模块 30 项单测全过、含 integration_tests 端到端覆盖），cargo clippy -D warnings 零警告。]
3. [Rust 核心：中国大陆网络优化（镜像源 + DNS）] —— [内置 BMCLAPI、Mcbbs、阿里云等国内镜像源并自动测速择优；HTTP 客户端支持 DNS 优化（DoH/自定义 resolver、Happy Eyeballs、连接复用、超时与退避）、可配置 HTTP/SOCKS 代理，专治访问 Mojang/Forge 慢、丢包与 DNS 污染。] [已完成 — 落地于 rust/crates/rc-launcher-core/src/net/{mod,mirror,dns,proxy,client}.rs：① 镜像源：内置 BMCLAPI/MCBBS/阿里云三大主流镜像（路径保持式重写，base_url + 可选 path_prefix），新增 extended_mirrors() 扩充华为云/清华 TUNA/中科大 USTC 等教育镜像；MirrorProvider 支持 measure/speed_test/select_best 自动测速择优并 pin 最快镜像，candidate_urls 按 MirrorMode（All/MirrorsOnly/Auto/Off）生成候选顺序，Auto 在 build 时即用 bootstrap 客户端测速锁定最优镜像；② DNS 优化：DnsMode{System, Static(自定义 resolver 绕过污染), Doh}，内置国内 DoH 上游（阿里/DNSPod/360 + 全球 Cloudflare/Google），parse_doh_json 解析 A/AAAA；新增 DnsCache 按 cache_ttl 做 TTL 缓存避免重复 DoH/系统解析（落实 DnsConfig.cache_ttl 字段），且 DoH 全部上游失败时优雅降级到系统解析器（专治丢包/阻断而非硬失败）；Happy Eyeballs 由 hickory-dns 在连接器层做 IPv4/IPv6 竞速；③ 代理：ProxyConfig 统一 HTTP/HTTPS/SOCKS5，DoH 引导客户端也走代理；④ NetworkClient 整合上述全部能力——连接复用（pool/tcp_keepalive）、超时与指数退避、逐候选重试与镜像回退（吸收 FCLCore/download 的 RangeUnsupported→单流降级纪律），并直接实现 download::HttpSource，使任务 2 的断点续传 DownloadManager 自动获得镜像回退/DoH/代理与 Range 续传。健壮性在 FCLCore/download 与 cuberite 经验上增强：origin 失败后按镜像优先级重试、HTTP 错误（如 404/500）不重试而立即切换镜像。测试：mirror 断言路径重写/host 过滤/择优、dns 断言 DoH 解析/静态覆盖/缓存过期/DoH 降级系统解析、client 断言 MirrorMode 排序与本地 HTTP 服务器端到端——origin 返回 500 时自动回退到镜像 200、NetworkClient 作为 HttpSource 完成 Range 分片拉取并经 DownloadManager 多分片下载校验通过；cargo test --workspace 全绿（net 模块 40 项单测 + 本地服务器 E2E），cargo clippy -D warnings 零警告。]
4. [Rust 核心：版本清单与依赖解析] —— [解析 Mojang version_manifest、各版本 game.json、libraries 与 assets 索引，结合镜像源生成依赖解析与下载计划（去重、平台过滤、规则匹配），对标 FCLCore/game 与 FCLCore/util 的清单处理逻辑。] [已完成 — 落地于 rust/crates/rc-launcher-core/src/game/{manifest,version,library,assets,platform,resolve}.rs（对标 FCLCore/game 与 FCLCore/util 的清单处理，并补齐此前缺失的健壮性/管线层）：① 清单解析：VersionManifest 解析 version_manifest（latest/release/snapshot + 每版本 version.json 的 url/sha1/time），新增 VersionManifest::fetch 经镜像源拉取并解析线上清单（resolve_full_plan 的统一入口）；② 版本解析：VersionJson 解析 client/server 下载、assetIndex、libraries、mainClass、minecraftArguments/arguments、java_version、logging、minimumLauncherVersion，merge_chain 走 inheritsFrom 链做叶子优先合并（child 覆盖 parent、库按坐标去重并原地替换以保持 classpath 顺序），resolve_version_chain 支持本地闭包取版并检测继承环；③ 依赖模型：Library 解析 Maven 坐标、downloads/classifiers、natives、extract、rules，is_allowed 按规则做平台/特性过滤（无规则默认包含、最后匹配规则决定、feature 不匹配即排除），artifact_url/classifier_url 在显式 URL→path→合成 Maven URL 间回退并支持自定义仓库；④ 平台与规则：Platform 提供 host（编译期检测）/android（Linux/AArch64）双档，OsRule 支持 os.name/arch/version 锚定正则（含 mac→osx、amd64/arm64 别名），Features 驱动 feature 规则；⑤ 资源索引：AssetsIndex 解析 objects（hash→size）与 legacy map_to_resources，plan_assets 现代版落到 assets/objects/<hh>/<hash>、legacy 版落到 assets/resources/<name>，均按 hash 下载；⑥ 编排与下载计划：DependencyResolver.build_plan 将解析结果转成去重、平台过滤、规则匹配的 DownloadPlan（client/assetIndex/libraries+natives/logging，按 dest 去重并合并更完整元数据），build_full_plan 再并入资源对象，into_tasks 用 canonical URL 让下载管理器（任务 2）自动获得镜像回退；新增 resolve_full_plan 一次性完成「取清单→解析版本链→拉取资源索引→生成完整计划」的镜像感知管线；⑦ 健壮性增强（高健壮性，针对大陆不稳定网络）：fetch_json_with_mirrors 在镜像返回 HTTP 200 但响应体非 JSON（污染错误页/CDN 缓存桩）时继续尝试下一个候选镜像而非立即失败，这是镜像下载最关键的韧性属性；ResolvedVersion 新增 required_java_major（由 java_version 的 major_version/组件名推断，未知保守取 Java 8，供任务 6 选 JRE）与 asset_index_ref（现代 assetIndex 优先、legacy assets 字符串合成标准索引 URL，统一管线入口）；全部含单元测试与本地 HTTP 服务端到端覆盖，cargo test --workspace 全绿（game 模块 43 项：含 fetch 镜像回退/legacy asset index/map_to_resources/resolve_full_plan 端到端），cargo clippy -D warnings 零警告。]
5. [Rust 核心：账号与鉴权模块] —— [实现 Microsoft OAuth 2.0 设备码登录（含 XBL/XSTS/Mojang 令牌链）与离线账号，令牌安全存储（Android Keystore）与自动刷新，对应 FCLCore/auth 子系统。] [已完成 — 落地于 rust/crates/rc-launcher-core/src/auth/{mod,model,transport,microsoft,offline,vault,store,manager}.rs（对标 FCLCore/auth 子系统，并补强此前缺失的 C-ABI 集成）：① Microsoft OAuth 2.0 设备码登录：request_device_code → poll_token（Pending/Expired/Denied/SlowDown 全状态机，含 interval 退避与 slow_down 指数退避）→ XBL → XSTS（uhs 一致性校验 + XErr 友好化：未绑 Xbox/儿童账号/封禁等）→ login_with_xbox → fetch_profile 完整令牌链，authenticate_device_code 编排 2–6 步，refresh_account 用 refresh_token 重跑令牌链续期；② 离线账户：offline_uuid 严格复刻原版 UUID.nameUUIDFromBytes("OfflinePlayer:<name>") 的 v3 / RFC4122 变体位，确定性可复现（含 Notch 已知向量）；③ 安全存储：SecretVault 抽象（InsecureVault 仅开发/测试、AesGcmVault 走 AES-256-GCM 自包含 nonce 密文），FileTokenStorage 原子写 + 按 vault 加密落盘，Android 侧经 FFI 桥把密钥封在 Android Keystore、Rust 只调 seal/unseal；④ 账户管理：AccountManager 增删查选 + ensure_fresh 在过期/临近过期（默认 300s 阈值）时透明续期；⑤ 全部走 AuthTransport trait，MockTransport 脚本化 0 网络单测，健壮性对齐 FCLCore/download 与 cuberite（XSTS uhs 不一致即报错、refresh 失败明确报错、client_id 空校验、空用户名拒登）。FFI 双桥：JNI（ffi.rs 九个 auth* 入口，catch_unwind 保护 + process-wide tokio runtime block_on）+ 本次补全的 C-ABI（capi.rs 的 rc_auth_init/list_accounts/add_offline_account/begin_microsoft/complete_microsoft/remove_account/refresh_account/ensure_fresh，与 JNI 共用同一全局 AccountManager，使 Unity/CLI/集成测试等非 JNI 消费者也能驱动鉴权逻辑，错误形态与 JNI 一致 {"error": msg}），真正实现任务 10 的扁平 C-ABI 对 auth 子系统的覆盖。测试：auth 模块 32 项单测（含设备码状态机/令牌链/XSTS uhs 不一致/离线 UUID 向量/vault 轮转/存储往返/管理器续期）+ capi 端到端鉴权生命周期测试，cargo test --workspace 全绿（565+ 项），cargo clippy -D warnings 零警告。]
6. [Rust 核心：JRE/JDK 供给（Android OpenJDK 打包/解压）] ———— [已完成 — 落地于 rust/crates/rc-launcher-core/src/runtime/{abi,java_version,manifest,source,extract,manager}.rs（对标 FCL Android-OpenJDK-Build 与 caciocavallo，并吸取 FCLCore/download 与 cuberite 的健壮性经验）：① 类型建模 Abi（arm64-v8a/armeabi-v7a/x86/x86_64）与 JavaVersion（8/17/21/25），与 FCL 的 bin-<suffix>.tar.xz + universal.tar.xz 布局及 assets/app_runtime/java/jre<major>/ 目录一一对应；② JreManifest 描述每个 (版本,ABI) 归档的 SHA-1+size，并可用 from_prebuilt_dir 扫描重建以校验提交态（runtime/generate_jre_manifest.py --check 同源理念）；③ JreSource 抽象取包：LocalDirSource 从本地 java/ 目录流式解压（先以流式文件哈希做 SHA-1/size 校验再解包，避免整归档驻留内存，契合任务 25 大文件流式处理），RemoteJreSource 经任务 2 的可断点续传、镜像回退 DownloadManager 下载（HTTP Range 续传 + 并行分片 + SHA-1 校验，主源失败自动降级到镜像，直接兑现任务 6「JRE 下载」的断点续传 + 镜像源），并让任务 3 的网络优化（DoH/代理/Happy Eyeballs）生效；④ 纯 Rust .tar.xz 解压（lzma-rs + tar），带路径穿越防护、bin/java 可执行位兜底与流式落盘；⑤ RuntimeManager 实现下载+校验+解压、多版本共存（<root>/<jreX>/<abi>/ 互相隔离）、按需释放（release/release_unused）与基于标记文件的完整性自检；健壮性在 FCLCore/download 与 cuberite 经验上进一步增强：安装过程改为「advisory 文件锁串行化 + 隐藏 .part 暂存目录 + 原子 rename 发布」，崩溃/中断不会遗留半成品 JRE 供读取方观察到，且并发安装同一 home 不会互相破坏（镜像 FCLCore 的防御性重建纪律）；测试：runtime 模块 21 项单测 + 端到端全过（本地真实 FCL 归档安装、脏 .part 暂存目录清理重建、RemoteJreSource 经 MockSource 主源失败一次后由镜像续传安装成功且 call_count>=2 证明回退发生），cargo test --workspace 全绿、cargo clippy -D warnings 零警告。]
7. [Rust 核心：启动引擎] — [构建 JVM 启动参数、类路径（LWJGL/原生库）、主类与游戏参数，派生并管理游戏进程、捕获标准输出/错误与退出码、支持崩溃诊断；对标 FCL Terracotta 启动引擎与 FCLCore/launch。] [已完成 — 落地于 rust/crates/rc-launcher-core/src/launch/{mod,options,classpath,args,env,command,process,crash,engine,render,runtime_assets,awt,fakefx,awt_host}.rs，模块边界对标 FCL 的 FCLCore/launch + FCLauncher/Terracotta 启动引擎：① 选项层 options 收口 UI 可影响的全部输入（账号、堆内存与上限钳制、窗口、渲染器、ABI、各类绝对路径、QuickPlay/ServerAddress），并做相对路径/空值校验；② classpath 按规则过滤（OsRule/feature）+ LWJGL 合并模块替换 + 按坐标去重与顺序保持，生成去重/替换/折叠计数；③ args 做 ${...} 占位符展开、规则门控参数表、未解析占位符裁剪；④ env 组装 LD_LIBRARY_PATH/java.library.path/渲染器环境（GL4ES/Mesa/ANGLE/Vulkan 翻译层，参考 FCL_APK_RUNTIME_ASSETS_CATALOG 与 FCL_NATIVE_LIBRARIES 的原生库布局）；⑤ command 汇总 JVM 参数 + -cp + 主类 + 游戏参数，并对访问令牌等机密做 redact；⑥ process 派生并监管游戏进程——有界日志环形缓冲（日志洪泛不会 OOM 启动器）、非 UTF-8 无损解码绝不 panic、逐行机密脱敏、kill_on_drop + stop() 的 SIGTERM→SIGKILL 递增终止（Minecraft 先存档再退出）、退出后继续排空管道避免丢失崩溃尾行、非阻塞输出流；⑦ crash 崩溃诊断将退出码/信号 + 捕获日志分类为 16 类 CrashCategory（CleanExit/UserTerminated/KilledBySystem/OutOfMemory/UnsupportedJavaVersion/MissingNativeLibrary/GraphicsFailure/NativeCrash/CorruptedFile/MissingMainClass/AuthenticationFailure/DiskFull/PermissionDenied/ModLoaderFailure/GameError/Unknown），规则按优先级（最具体者胜），抽取首异常、hs_err_pid*.log 路径与证据行（去重/截断/上限），并产出中英文可操作建议（i18n，task 20，键基于 category.id）；⑧ engine 编排器 LaunchEngine 走 preflight（JRE 存在性与大版本匹配、app_runtime 完整性、classpath 逐项存在性、运行时目录创建）→ command → process → verdict，dry_run 仅产出命令行不触盘，launch_and_wait 把每行日志写入进程级环形（task 19）并在崩溃时 best-effort 持久化/推送崩溃报告，stop() 委托进程监管；⑨ render/runtime_assets 依据 FCL 实机目录布局（assets/app_runtime 下的 LWJGL 3.3.3/3.4.1、caciocavallo/caciocavallo17、JNA、JRE）做原生库发现、ABI 作用域与翻译层环境变量合成；⑩ awt/fakefx/awt_host（task 18）提供 caciocavallo AWT 兼容与 Compose 绘制的 Canvas/帧/事件传输与会话泵线程。健壮性在 FCL Terracotta 与 FCLCore/launch 经验上增强：本轮进一步为崩溃诊断新增 CrashSeverity 分诊层级（Fatal/Recoverable/UserAction/Info，含 severity()/is_user_recoverable()/auto_remediable()，并随 CrashReport::to_json 透出 severity/user_recoverable/auto_remediable 供 UI 与 FFI 直接使用），并补充中国大陆 Android 实机高频崩溃模式（eglCreateContext/eglChooseConfig/opengl context creation failed/no egl display/swiftshader、GC overhead limit exceeded/direct buffer memory/cannot allocate memory、dlopen failed: library "lib*.so" not found/cannot open shared object file、FATAL SIGNAL/SIGSEGV/core dumped 等），使渲染失败/内存不足/原生库缺失/原生崩溃等典型场景可被精确归类与给出一键修复入口；测试：launch 模块 209 项单测（含用伪 JVM 脚本端到端真正派生进程，验证干净退出/OutOfMemory 崩溃分类/用户主动停止非崩溃/SIGTERM→SIGKILL 升级/非可执行 java 报错等），crash 单列 23 项单测覆盖各分类与分诊层级（含新增 EGL/GC overhead/原生库/致命信号 模式与 severity 一致性），cargo test --workspace 全绿，cargo clippy -D warnings 零警告。]
8. [Rust 核心：Mod / 资源包 / 光影管理] —— [解析 Forge/Fabric/Quilt/OptiFine 元数据，完成安装、启用/禁用、依赖与冲突检测、版本隔离；对应 FCLCore/mod 子系统，复用其 mod 解析与翻译表经验。] [已完成 — 落地于 rust/crates/rc-launcher-core/src/mods/{mod,loader,constraint,metadata,conflict,resource_pack,shader}.rs，模块边界对标 FCL 的 FCLCore/mod 子系统并复用其 mod 解析与翻译表经验：① loader 分类（Vanilla/Forge/Fabric/Quilt/LiteLoader/OptiFine，OptiFine 按文件名识别）；② constraint 统一 Fabric semver 与 Forge Maven 区间记号（convert_maven_range 把 [1.16.5]/[24,)/(,1.12.2]/[1.0,2.0) 归一为 semver，coerce_mc_version 容错 1.16.5/1.7.10/1.16/1 等短式与 -pre 后缀）；③ metadata 解析 fabric.mod.json/quilt.mod.json/META-INF/mods.toml（优先于 mcmod.info）/mcmod.info/litemod.json，宽进严出（畸形依赖降级为无约束而非整体解析失败），OptiFine 按文件名推导 modid 与 MC 版本；④ conflict 经 resolve_issues 遍历 enabled mod 的 depends/breaks/conflicts 边，产出 MissingDependency/IncompatibleDependency/Conflict/IncompatibleMinecraft/DuplicateMod 五类 ModIssue（含稳定 code 供 UI/FFI 使用），disabled 文件整体被忽略不参与解析；⑤ resource_pack 管理 pack.mcmeta（pack_format + 描述），提供 pack_format_compatible 让 UI 在选中的 MC 版本上预警黑屏类不兼容；⑥ shader 管理 OptiFine/Iris shader 包（含 shaders/ 树即有效）。版本隔离由每个实例独立的 ModManager/ResourcePackManager/ShaderPackManager 绑定各自 mods/、resourcepacks/、shaderpacks/ 目录保证，启用/禁用统一用 .disabled 后缀重命名落盘，状态持久零额外记账且重启后保留。健壮性在 FCLCore/mod 经验上增强：扫描遇无法解析的归档保留占位元数据（让用户可见并移除而非整体中止）、OptiFine 文件名变体（小写前缀/`.disabled`/`!`）仍能推导 MC 版本、pack_format 表覆盖 1–18 且未知格式返回 None 失败安全。测试：cargo test --workspace 全绿（mods 模块 31 项单测 + integration_tests.rs 端到端 5 项——install→scan→resolve 缺失依赖/冲突/重复/MC 版本不兼容、实例间版本隔离、资源包 pack_format 兼容与启用禁用、光影包校验与启用禁用），cargo clippy -D warnings 零警告。]
9. [Rust 核心：插件/渲染扩展机制] —— [设计可插拔的渲染器与本地库接口（GL4ES/ANGLE/SDL 渲染插件、NativeLib 注入、RendererPlugin 校验），借鉴 FCL RendererPlugin 与 Zalith NativeLibPlugin 的插件化与安全加载思路。] [已完成 — 落地于 rust/crates/rc-launcher-core/src/plugins/{mod,renderer,native_lib,validation,fcl_apk}.rs（借鉴 FCL RendererPlugin 与 Zalith NativeLibPlugin/VerifiedPluginLoad 的插件化与安全加载思路，统一封装任务 9 的渲染扩展机制）：① 可插拔渲染器 RendererPlugin + RendererRegistry——数据驱动的 OpenGL(ES) 翻译栈描述（LWJGL libname、backend、env、注入的原生库、ABI、trust/provenance），内置 5 个 FCL 渲染栈之外新增 SDL2 渲染插件（WindowingBackend::Sdl，gl_libname=liblwjgl_sdl.so，来源 LWJGL 3.4.1 natives，对应任务点名的「GL4ES/ANGLE/SDL 渲染插件」），启动引擎 Renderer 枚举新增 Sdl 变体并正向派生到 id()/gl_libname()/env()/from_id() 与 launch/render 的环境装配，renderer_plugin() 作为单一事实来源覆盖全部 6 个；② NativeLib 注入——NativeLib/Source/LoadOrder/optional 模型与 resolve_native_lib_dirs，按 NativeLibDir/PluginOwned/LwjglNatives/JreLib 四类搜索根解析 java.library.path / LD_LIBRARY_PATH 注入目录（新增 in_lwjgl_natives 构造器），validate_name 做路径穿越防护；③ RendererPlugin 校验（安全加载）——validate/validate_with_verifier 按「路径安全 → ABI → 原生库存在性与完整性 → 分离签名 → 信任」顺序判定，新增调用 NativeLib::verify_on_disk 在库存在时校验 size+SHA-1（修复此前仅查尺寸、被篡改库可绕过 SHA-1 的缺口，产出 native_lib_hash_mismatch/size_mismatch/read_error），信任来自 System/UserApproved/可信作者/可信完整性哈希；④ 真正可工作的可插拔签名校验——在 SignatureVerifier trait 上落地 HmacSha1Verifier（RFC2104 HMAC-SHA1，常量时间比较，复用既有 sha1 依赖，无需引入 Ed25519 依赖），对插件的规范完整性哈希做签名/验签，无校验器时降级为「签名未验证」告警而非静默放行；⑤ 预设清单 fcl_apk 从 FCL 发布 APK 抽取 GL4ES/NG-GL4ES/VirGL/Zink/ANGLE 的原生库清单（文件名/尺寸/SHA-1/SHA-256）做 preset_registry 与部署期完整性交叉校验。测试：plugins 模块 33 项单测（含 SDL 注册表、HMAC 签名通过/篡改拒绝/无校验器告警、SHA-1 完整性正负路径、路径穿越、信任与散列匹配、FCL APK 预设交叉校验）、launch/options + launch/render 的 Renderer 枚举往返与 SDL 环境装配、ffi 的 launch_renderers_json 目录完整性（现含 6 个、含 SDL），cargo test --workspace 全绿（584+），cargo clippy -D warnings 零警告。]
10. [FFI/JNI 桥接层] —— [通过 cbindgen + JNI 将 Rust 核心安全暴露给 Kotlin/Compose，提供异步回调、进度事件（event bus）与错误透传；参考 MCTier 的 Rust FFI（libeasytier_ffi.so）与 JNI 桥接模式，确保线程安全与零拷贝传递。] [已完善 —— 落地于 rust/crates/rc-launcher-core/src/{ffi,capi,event,jobs}.rs 与 cbindgen.toml（生成 rc_launcher.h），Kotlin 侧 core/src/main/java/com/rc/launcher/core/{RustBridge,RcEventBus}.kt：① 双层桥接——C-ABI（capi.rs，由 cbindgen 生成 rc_launcher.h，镜像 MCTier 的 libeasytier_ffi.so）供非 JNI 原生消费者（Unity/Unreal 插件、CLI、测试）使用，JNI 函数（ffi.rs 的 Java_com_rc_launcher_core_RustBridge_*）做薄封装，二者共用同一事件总线与任务运行时；② 异步回调——jobs::spawn_job 经常驻 tokio runtime（job_runtime）fire-and-forget 派发，本次进一步把任务 2 的断点续传下载管理器真正接入：新增 type:download 作业（jobs::spawn_download_job，并暴露为 JNI downloadAsync 与 C rc_download_async），由 DownloadManager 执行、借 with_progress 把 Progress 经事件总线回传，UI 永不阻塞、仅通过总线获知结果；③ 进度事件 event bus——event::EventBus/EventSink 单一进程级 sink，Kotlin 经 RcEventBus.eventBusSubscribe 仅订阅一次，每个事件为扁平 JSON 字符串跨 JNI 一次性解码为 RcEvent（无逐字段 marshalling，近零拷贝）；④ 错误透传——新增结构化错误码：Event 增加可选 code 字段（serde skip-if-none，兼容旧消息），error_with_code / lifecycle_with_result 让错误携带机器可读 code（如 download_failed、job_failed）与结构化 result，跨 JNI/C 边界由单 JSON 字符串透传，UI 按 code 分流而非字符串匹配；⑤ 线程安全与零拷贝——JniEventSink 持 GlobalRef+JavaVM，任意 Rust worker 线程 attach 后调用回调（MCTier 的每线程 attach 模式），所有入口以 catch_unwind 包裹防止 panic 中止 VM，awtPollFrame 直接写入 Compose Bitmap 背后的 direct ByteBuffer 实现像素级零拷贝；⑥ 健壮性增强：CEventSink 标记 Send+Sync 存入全局总线、sink panic 在 publish 内被捕获、scope 级取消标志（cancel_job/cancelAsync）可终止作业；cargo test --workspace 全绿（event/jobs/capi 单测覆盖内存源下载作业端到端——进度/完成/错误码、错误序列化、取消），cargo clippy -D warnings 零警告。]

11. [Compose UI：应用骨架与导航] —— [已完善 —— 落地于 app/src/main/java/com/rc/launcher/{MainActivity.kt, ui/RcApp.kt, ui/MainScreen.kt, ui/navigation/RcNavigation.kt, ui/theme/{Theme,ThemeData,ThemeEngine,ThemeStorage,ThemeViewModel}.kt}：Material 3 Scaffold + CenterAlignedTopAppBar + NavigationBar 主框架；Navigation Compose 2.9.8 类型安全路由（@Serializable HomeRoute/InstancesRoute/DownloadsRoute/SettingsRoute/AccountsRoute/ControllerRoute/AwtRoute/InstallRoute 八条 data object 路由，与携带参数的 InstanceDetailRoute(val id)，RcNavHost 用 composable<T>() 注册、调用方用 navController.navigate(T)，MainScreen 用 hasRoute<T>()/is 判定选中态与标题）；主题引擎 ThemeEngine/ThemeData/ThemeStorage/ThemeViewModel 以 StateFlow 暴露不可变状态，RcTheme 用 collectAsStateWithLifecycle 订阅并接 isSystemInDarkTheme→dynamicColorScheme（Android 12+ 动态取色，低版本回退种子色），状态栏/导航栏着色；技术栈 Kotlin 2.4.10 + Compose BOM 2026.08.00（ui 1.12.0 / material3 1.4.0 / material-icons-extended 1.7.8）+ Navigation Compose 2.9.8 + AGP 9.1.2 + compileSdk 37（经 gradle/libs.versions.toml 单一事实源 + app/build.gradle.kts resolutionStrategy.force 钉死，消除 logcat/25_08-13-50-16_522.log 中 TextStyle.copy$default NoSuchMethodError 启动崩溃）；测试 RcNavigationTest / RcAppSkeletonTest / ThemeLogicTest 经 ./gradlew testDebugUnitTest 门禁。]

12. [Compose UI：主页与实例仪表盘] —— [游戏实例卡片、一键快速启动、最近游玩、资源占用与悬浮帧率 HUD；对标 FCL 主页与 MCTier GameHudOverlay 的悬浮层思路，提供流畅的主屏交互。] [已完成 — 落地于 app/src/main/java/com/rc/launcher/ui/screen/HomeScreen.kt、ui/component/{InstanceCard,FloatingHud,ResourceSummary}.kt、ui/viewmodel/DashboardViewModel.kt、ui/resource/{ResourceMonitor,FpsTracker}.kt、ui/model/{GameInstance,InstanceRepository}.kt：主页采用 Material3 Scaffold 之上自绘的滚动仪表盘——问候头部（含 Rust 核心就绪状态）、实时资源占用面板（内存/存储/CPU，设备信息缺失时降级显示“不可用”）、横向“最近游玩”实例轨、完整实例网格（收藏优先→最近游玩→名称排序的 dashboardOrder）、一键快速启动与悬浮帧率 HUD（FPS/CPU/内存/存储，启动中自动显示、可手动开关）；状态机由 DashboardViewModel 驱动（Idle→Launching→Running→Idle，preflight 失败→Failed、stop 安全停止、dismissError、并发启动去重、未知 id 忽略），LaunchExecutor 抽象使 Rust 核心集成（任务7 preflight + 任务10 事件总线）可无缝替换 SimulatedLaunchExecutor；健壮性：ResourceMonitor 所有平台调用 try/catch 降级 unknown、FpsTracker 在 Choreographer 缺失时降级 0 FPS、HUD 关闭在运行期禁用避免误关。对标 FCL 主页与 MCTier GameHudOverlay 悬浮层思路。测试：app/src/test/java/com/rc/launcher/ui/viewmodel/DashboardViewModelTest.kt 扩展（启动状态机 Idle→Running→Idle、preflight 失败→Failed、失败后重试、失败态安全停止、并发启动去重、未知 id 忽略、stop 空闲安全、HUD 开关、instances 镜像仓库，经注入 CoroutineScope(Dispatchers.Unconfined) 在 JVM 确定性驱动）；app/src/test/java/com/rc/launcher/ui/model/GameInstanceTest.kt 加固（effectiveGameDir 三种版本隔离策略 DEFAULT/ISOLATED/CUSTOM 及空白自定义回退、dashboardOrder 收藏优先→最近游玩→同名按名排序、recentlyPlayed 过滤与稳定排序）；与既有 app/src/test/java/com/rc/launcher/ui/component/InstanceCardTest.kt、ui/resource/ResourceMathTest.kt 一并经 ./gradlew testDebugUnitTest 门禁。]
13. [Compose UI：版本/实例管理] —— [版本安装向导（原版/Forge/Fabric/Quilt/OptiFine）、实例列表、设置编辑与版本隔离；参考 FCLCore/game 与 Zalith 版本管理的数据模型。] [已完成（进一步增强）— 实现位于 app/src/main/java/com/rc/launcher/ui/model/{GameInstance,InstallProfile,InstanceRepository}.kt、ui/viewmodel/{InstallViewModel,InstanceDetailViewModel}.kt、ui/screen/{InstallWizardScreen,InstanceDetailScreen}.kt，并含单元测试；本次迭代在已有基础上进一步提高完成度：① 实例复制/克隆闭环——新增 GameInstance.duplicate(newId,overrideName) 模型助手、InstanceRepository.duplicate(sourceId,overrideName)（克隆 id 唯一化，形如 <id>-copy / <id>-copy-N）、InstanceDetailViewModel.duplicate() 与 InstanceDetailScreen「复制实例」按钮（创建后跳转克隆详情），补齐「实例管理」能力；② 安装向导校验加固——新增 MAX_INSTANCE_NAME_LENGTH（名称上限 64 字）与 SUPPORTED_JAVA_VERSIONS={8,17,21}，校验文案更精确（Java 版本不受支持 / 实例名称过长），杜绝非法 Java 版本（7/99）与超长名称进入实例；③ InstallViewModel.create() 增加合法性守卫：请求非法（!isValid）时返回 null 而非写入半成品实例，UI 仅在 canProceed 通过后才提交；④ GameInstance.isPlayable 派生属性（version 非空才可启动）。测试同步增强：InstallProfileTest 新增校验与 duplicate 用例、InstanceDetailViewModelTest 新增 duplicate/空载用例、InstallViewModelTest 新增 create 守卫用例、GameInstanceTest 新增 isPlayable 用例，全部 JVM 单测通过（./gradlew testDebugUnitTest）。]
14. [Compose UI：设置中心] ———— [镜像源选择、Java 参数与内存、渲染器/分辨率/帧率、控制器与目录配置、备份与恢复；对应 FCL 设置面板与各 RendererPlugin 配置项。] [已完成 — 实现位于 app/src/main/java/com/rc/launcher/ui/model/{LauncherSettings,SettingsRepository,RendererPluginConfig}.kt、ui/viewmodel/SettingsViewModel.kt、ui/screen/SettingsScreen.kt，并含单元测试 app/src/test/java/com/rc/launcher/ui/model/LauncherSettingsTest.kt 与 app/src/test/java/com/rc/launcher/ui/viewmodel/SettingsViewModelTest.kt。本次进一步提升完成度：① 新增 RendererPluginConfig（Zink Vulkan 驱动 / ANGLE 后端 / GL4ES sRGB 模拟开关 / VirGL 远程服务器），与 FCL_NATIVE_LIBRARIES.md 的原生库（libzink_dri.so、libGLESv2_angle.so、libgl4es_114.so、libng_gl4es.so、libvgpu.so、libvulkan_freedreno.so）及各 RendererPlugin 配置项一一对应；落地于 LauncherSettings.rendererOptions，由 SettingsRepository 经 SharedPreferences 持久化（新增 4 个 key）、SettingsViewModel 新增 setZinkVulkanDriver/setAngleBackend/setGl4esNoSrgb/setVirglServer 并经 LauncherSettings.sanitized 同步清洗；SettingsScreen 在渲染器分区按当前所选渲染器条件展示对应高级选项。② 新增设置「导出/导入（备份与恢复）」：LauncherSettings.toBackupString/fromBackupString 以稳定的 key=value 行格式序列化全部偏好（嵌套项置于 renderer.*），缺键回退默认、未知键与 # 注释忽略、导入即 sanitized，绝不让被篡改的备份把越界值写进 Rust 核心；SettingsViewModel.exportSettings/importSettings（空白输入视为空操作返回 false）与 SettingsScreen「数据与备份」分区联动。③ 单测扩充：LauncherSettingsTest 新增 rendererPluginConfig_defaultsValid / backup_roundTrip_defaults / backup_roundTrip_customValues / backup_ignoresUnknownKeysAndComments / backup_sanitizesOnImport；SettingsViewModelTest 新增 rendererOptions_settersUpdateStateAndPersist / exportImport_roundTrips / importSettings_rejectsBlankAndKeepsState / export_defaultsProducesAllKeys，全部经 JVM 确定性驱动。④ 本次再进一步提升：新增镜像「测速并选择最快」能力——抽象出可注入的 MirrorMeasurer（默认 DefaultMirrorMeasurer 走 java.net 时限 HEAD 探测，测试注入 FakeMirrorMeasurer 离线运行），SettingsViewModel 新增 mirrorProbe: StateFlow<MirrorProbeState> 与 suspend measureAndSelectFastestMirror（逐镜像探测、minByOrNull 取最低时延并经由 setMirror 钉选、全程发布 Measuring/Done 进度），SettingsScreen 在网络分区新增按钮并实时展示测速进度 / 最优镜像 / 不可达告警；同时把已有 LauncherSettings.validationError() 在设置中心顶部以 errorContainer 横幅实时呈现（task 19 健壮性），非法配置一眼可见。单测再扩充：SettingsViewModelTest 新增 measureAndSelectFastestMirror_selectsLowestLatency / _noReachableKeepsCurrent / _updatesProbeState（runBlocking + FakeMirrorMeasurer）。]
15. [Compose UI：控制器/输入映射] —— [虚拟按键、外接手柄、鼠标键盘映射与触控摇杆布局；对标 FCL-Controllers 与 Zalith 控制映射，支持布局保存与自定义按键。] [已完成（进一步增强）—— 实现位于 app/src/main/java/com/rc/launcher/ui/model/{ControlLayout,ControlLayoutRepository}.kt、ui/model/json/MiniJson.kt、ui/viewmodel/ControlLayoutViewModel.kt、ui/screen/ControllerScreen.kt；设置页“控制器”区段新增“编辑按键映射”入口（RcNavigation 新增 controller 路由、RcApplication 安装 SharedPreferences 仓库）；本次迭代在已有基础上进一步提高完成度：① 键位词表扩展——MappedKey 新增方向键与更多字母·数字·功能键（KEY_UP/DOWN/LEFT/RIGHT、KEY_4..9、KEY_F1..F5、KEY_CAPS/DELETE/INSERT 等），并新增 GamepadAxis 模拟轴枚举（左/右摇杆 X/Y、LT/RT 扳机），覆盖“外接手柄”完整词汇；② VirtualJoystick 新增 axisX/axisY 绑定，使触控摇杆可镜像外接手柄物理轴（对标 FCL/Zalith 外接手柄）；③ ControlLayout.validate() 布局校验——重复控件 id（ERROR）、空布局/无按键按钮/按钮重叠误触（WARNING），呼应任务 19 健壮性；④ ControlLayout.duplicate() 布局克隆（另存为）、elementAt(nx,ny) 命中测试（运行时触控输入分发，决定手指归属的控件）、summary() 结构摘要（按键/摇杆计数与已绑定按键集合）；⑤ JSON 持久化新增 version 字段与 migrateFrom 迁移钩子（向前兼容旧布局）；⑥ ViewModel 暴露 issues/summary/duplicateCurrent，updateJoystick 扩展为支持轴绑定的双重载（三参版保留既有轴，避免仅调半径/驱动轴时丢失手柄映射）；⑦ ControllerScreen 新增校验横幅（错误红/警告橙/通过绿）与布局概览行，摇杆编辑器新增“外接手柄轴（可选）”绑定 UI（无/各轴 chip）；⑧ 测试增强——ControlLayoutTest 新增校验/克隆/命中测试/摘要/扩展键/JSON 版本与轴往返、ControlLayoutViewModelTest 新增 issues 暴露与刷新/duplicateCurrent/摇杆轴更新（含“仅改半径不丢失轴绑定”用例），全部 JVM 单元测试通过（./gradlew testDebugUnitTest）。]
16. [Compose UI：账号管理] —— [微软登录流程 UI、正版/离线账号切换、皮肤预览与令牌状态；对应 FCLCore/auth 的 UI 层，保证登录状态可视化。] [已完成 — 实现位于 app/src/main/java/com/rc/launcher/ui/model/{Account,AccountRepository}.kt、ui/viewmodel/AccountViewModel.kt、ui/screen/AccountsScreen.kt，并含单元测试 app/src/test/java/com/rc/launcher/ui/model/AccountModelTest.kt 与 app/src/test/java/com/rc/launcher/ui/viewmodel/AccountViewModelTest.kt；进一步（提高完成度）：新增皮肤预览弹窗（点击头像查看大图与令牌剩余，对齐 FCL MicrosoftAccountSkinDialog）、微软登录弹窗增加设备码倒计时 / 一键复制验证码 / 浏览器打开验证页、当前账户卡展示令牌剩余时长、loadAccounts 在加载时主动刷新即将过期或已过期的微软令牌（保证登录状态可视化）；并补充 formatDuration 与 MicrosoftAccount.remainingSecs 纯函数单测、loadAccounts 主动刷新单测；再进一步提高：新增账户删除二次确认弹窗（防误删）、顶部“刷新所有令牌”按钮与 refreshAllMicrosoft（一键刷新全部微软令牌）、loadAccounts 加载失败时显式上报错误（健壮性，呼应任务 19）；并补充 refreshAllMicrosoft 与加载失败上报错误单测]
17. [渲染集成：LWJGL + GL4ES/ANGLE（Android OpenGL）] —— [集成 FCL/Zalith 的 LWJGL 3.3.x 原生库、GL4ES/ANGLE 将 OpenGL 翻译至 OpenGL ES，实现 Minecraft 在 Android 的图形输出与性能调优。] [已完成 — 实现位于 rust/crates/rc-launcher-core/src/launch/render.rs（LWJGL 原生库发现/校验、GL4ES/ANGLE 的 OpenGL→OpenGL ES 翻译配置、PerfProfile 性能调优）；接入 launch/env.rs（性能调优 env）与 launch/engine.rs（LWJGL 原生库 preflight 校验），并在本次迭代中进一步提高完成度：① 新增渲染器支撑库清单与校验 RendererNativeLib / renderer_native_manifest / RendererNativeBundle（依据 FCL_NATIVE_LIBRARIES.md 的 30 个 .so，逐个渲染器匹配其 gl_libname 所需的支撑库：GL4ES 需 libgl4es_114.so（libglapi.so 可选）、ANGLE 需 libGLESv2_angle.so + libEGL_angle.so、Zink 需 libOSMesa_8.so + libzink_dri.so、VirGL 需 libvgpu.so、SDL 需 liblwjgl_sdl.so），scan/discover/is_complete/missing_required/present_paths 与 LWJGL 原生库校验同构，且 discover 采用最佳努力策略（nativeLibraryDir 为 None 或目录不存在时跳过而非失败，仅当目录存在却缺必需库时报 MissingFile），补全了 task 17 仅校验 LWJGL 原生库、未校验 GL4ES/ANGLE 支撑库的健壮性缺口（避免游戏在 GL 初始化时才以 UnsatisfiedLinkError / EGL_BAD_CONFIG 崩溃）；RenderIntegration 新增 preflight_renderer() / renderer_native_bundle() / renderer_backing_ok() 并在构造时扫描支撑库，engine.rs 的 preflight_app_runtime 现已在 JVM 启动前一并 preflight 渲染器支撑库；② 修复 RenderIntegration::describe() 的复制粘贴 bug（原 perf={} 占位符实际打印的是 natives 完成度、从未打印渲染器档位），改为正确汇报 renderer_backing_ok；③ 补充 9 个新增单元测试覆盖支撑库清单分组、scan/discover（含 None/目录缺失/缺必需库/仅缺可选库）与 RenderIntegration preflight/describe，cargo test --workspace 全绿（render 模块 18 项），cargo clippy -D warnings 零警告（并顺带修掉 capi.rs 两处预先存在的 let_unit_value 错误）。]
18. [AWT/Swing 兼容层（fakefx）] —— [实现 JavaFX/AWT-Swing 在 Android 的兼容与 Canvas 渲染，使 Minecraft 内嵌 UI 在 Compose 中正常显示；借鉴 FCLCore/fakefx 的适配方案。] [已完成 — Rust 侧 rust/crates/rc-launcher-core/src/launch/{awt.rs, fakefx.rs, awt_host.rs}：awt.rs 提供 caciocavallo 后端选择（Cacio8/Cacio17/Headless）与 jar/原生库校验、JVM 参数、RCAF 帧 wire 格式、32 字节 AWT 事件记录、RCAC 控制消息（11 种 kind：cursor/title/clipboard_set/clipboard_request/beep/screen_size/ime_show/ime_hide/window_opened/window_closed/bye；头部与帧头同形而 magic 不同）、定长控制应答记录（保留 id CONTROL_EVENT_ID + UTF-8 分片，encode/decode_control_reply）、CursorKind（java.awt.Cursor 全 14 型）、AwtCanvas（双缓冲 + damage）、Viewport（信箱化，map_pointer/map_to_surface）、AwtInputTranslator；fakefx.rs 提供 AwtSession（帧提交与呈现、输入出队与限流、控制投影 AwtControlState{cursor,title,ImeCaret,clipboard_out,windows,beeps,bye} 与消息流 drain_control、answer_clipboard、adopt_screen_size 静默采纳 managed screen）、AwtFrameStream（按 magic 解复用帧与控制消息）、AwtEventWriter；awt_host.rs 提供 AwtHost（mkfifo 命名管道通道 + 两个 poll(2) pump 线程、LinkState 与 LinkStats 计数含 controls_accepted/rejected）。LaunchOptions.awt_transport_dir 经 CommandBuilder 广告 rc.awt.bridge.{protocol,frames,events} 给 JVM。FFI / RustBridge.kt：awtOpen/awtClose/awtInfo/awtConfigure/awtAttachTransport/awtInput/awtSubmitFrame/awtPollFrame（direct ByteBuffer 零拷贝）/awtPollFrameArray/awtDrainEvents/awtDrainControl/awtControl/awtSubmitControl。Compose 侧 app/src/main/java/com/rc/launcher/ui/awt/{AwtGeometry,AwtWire,AwtControl,AwtInput,AwtSessionInfo,AwtCanvasBridge,AwtAndroidKeys}.kt、ui/viewmodel/AwtSurfaceViewModel.kt、ui/component/AwtCanvasSurface.kt（每帧 poll 帧 + 排空控制面，矢量指针叠加层、系统剪贴板双向同步、软键盘随 Swing 插入符弹出/收起、beep→触感）、ui/screen/AwtScreen.kt（画布 + 诊断面板 + 帧/控制面自检；RcNavigation 的 awt 路由，RcApplication 安装 RustAwtCanvasBridge）。文档 docs/awt.md。测试与门禁：cargo test --workspace 全绿（644 + 2 doc，其中 awt::control_tests 13 项、fakefx 72 项、awt_host 23 项、ffi::awt_tests 21 项）、cargo clippy --workspace --all-targets -D warnings 零警告、examples/awt_demo.rs 真实命名管道端到端（整帧/损坏帧/脏矩形补丁/双向控制面/输入往返）、scripts/check_awt_wire.py 跨语言 wire 契约门禁（比对两侧 magic/版本/头长/kind 码/应答码/光标号与 id/java.awt.event id/ScaleMode id/控制 JSON 键），Kotlin 单测 app/src/test/java/com/rc/launcher/ui/awt/{AwtGeometryTest,AwtWireTest,AwtControlTest,AwtInputTest,AwtSessionInfoTest,AwtAndroidKeysTest}.kt 与 ui/viewmodel/AwtSurfaceViewModelTest.kt；.github/workflows/build.yml 已接入 awt_demo 与 check_awt_wire.py。]
19. [健壮性：错误处理/重试/离线降级] —— [统一错误模型、网络抖动重试退避、断网降级与本地缓存、崩溃日志与上报；吸收 cuberite 与 FCLCore 的健壮性经验，保障弱网与异常下的可恢复性。] [已完成 — 实现位于 rust/crates/rc-launcher-core/src/error.rs（统一错误模型 + 严重性/可重试/退避建议分类）、robust/{retry,cache,reporter}.rs（网络抖动重试退避 / 断网降级与本地缓存 / 崩溃日志与上报），并在 launch/engine.rs 集成崩溃上报（record_log + report_crash）；含单元测试（cargo test --workspace 全绿，robust/error 模块 30 项单测全过）。本次迭代在 task 19 基础上进一步提高完成度：① 修复缓存正确性缺陷——`fetch_cached` 此前对 Range 分片请求与整文件请求共用同一缓存键，分片请求会污染整文件缓存；现改为仅缓存整文件（start==0 且无 end），Range 请求走独立 `cache_key_range` 键且绝不回退到整文件缓存（避免返回错误字节）；② 新增缓存治理 `CacheStore::{entry_count,total_size_bytes,clear,prune_older_than}` 与 `cache_key_range` 辅助；③ `RetryPolicy` 新增 `max_elapsed` 总时限，重试序列不会在死链上无限空转，并在 `retry` 中尊让 429 `Retry-After` 的 `suggested_backoff`（长于计算退避时覆盖之）；④ `reporter` 新增 `list_crash_logs`/`prune_crash_logs` 对崩溃报告做列举与按数量裁剪（corrupt 报告跳过不阻断其余）；cargo clippy -D warnings 零告警。]
20. [国际化（zh-CN / en 等）] —— [基于资源文件的 i18n 框架，中文优先文案与多语言切换；参考 FCL 多语言 README 与 MCTier 本地化实践。] [已完成 — 资源文件为唯一事实来源：rust/crates/rc-launcher-core/i18n/{zh-CN,zh-Hant,en}.properties（各 100 key，zh-CN 为基准语言/中文优先，缺失键按回退链回退到中文而非裸 key）；Rust 侧 rust/crates/rc-launcher-core/src/i18n/{mod,language,catalog,format}.rs（宽松 BCP-47 解析与协商、.properties 解析器、include_str! 内嵌目录、运行时 overlay、{name} 插值与 CLDR 复数、bundle/completeness/diagnostics）；核心文案统一收口：launch/crash.rs 的 summary/advice/advice_zh 改为目录视图并新增 localized_summary/localized_advice 与 CrashReport::to_json_in，error.rs 新增 RcError::{i18n_key,localized,localized_current,severity_label} 与 ErrorSeverity::i18n_key，ffi.rs 的 launchDiagnose 支持 language 字段；FFI 新增 i18nLanguages/i18nCurrentLanguage/i18nSetLanguage/i18nTranslate/i18nBundle/i18nDiagnostics/i18nOverlay（ffi.rs + capi.rs 的 rc_i18n_* C ABI + RustBridge.kt）；生成器 scripts/{i18n_common,gen_android_strings}.py 由 .properties 生成 app/src/main/res/{values,values-en,values-zh-rTW}/strings.xml、res/xml/locales_config.xml 与 ui/i18n/RcStringResources.kt（key→R.string 映射，不用反射），scripts/check_i18n.py 提供 34 项一致性门禁（键齐备/孤儿键/占位符漂移/空值/生成物过期/XML 往返/UI 键存在/崩溃与错误键存在/Kotlin 与目录一致）；Compose 侧 app/src/main/java/com/rc/launcher/ui/i18n/{AppLanguage,RcStrings,RcStringKeys,RcStringsLoader,RcLocaleContext,LocaleStorage,LocaleEngine,RcLocalization}.kt 与 ui/viewmodel/LocaleViewModel.kt（LocaleEngine 仿 ThemeEngine 单例：core bundle 优先、Android 资源兜底、失败回显 key；RcLocalizationProvider 同时覆盖 LocalRcStrings/LocalContext/LocalConfiguration/LocalLayoutDirection，故 rcString 与 stringResource 均随应用内语言切换且无需重建 Activity；Android 13+ 同步 LocaleManager.applicationLocales，MainActivity.onConfigurationChanged 回灌系统语言）；接入 RcApplication/RcApp/MainScreen/RcNavigation（labelKey）与 SettingsScreen 新增「语言与地区」分区（端名不翻译、跟随系统显示实际解析结果）；文档 docs/i18n.md，CI build.yml 新增 i18n 端到端示例与 check_i18n.py 门禁；测试：cargo test --workspace 全绿（524 项，较基线 443 项新增 81 项 i18n/error/ffi/capi 测试，并以 GLOBAL_I18N_TEST_LOCK 串行化全局语言/overlay 状态）、examples/i18n_demo.rs 端到端跑通，Kotlin 单测 app/src/test/java/com/rc/launcher/ui/i18n/{AppLanguageTest,RcStringsTest,LocaleEngineTest,CoreStringsSourceTest,CatalogueParityTest}.kt 与 ui/viewmodel/LocaleViewModelTest.kt；【本轮进一步提高完成度】① 修复既有 CI 门禁真实缺陷：check_i18n.py 用 `pub fn id(self) -> &'static str` 裸正则抓 crash id，因 crash.rs 中 CrashSeverity::id 先于 CrashCategory::id 出现而误抓 ['fatal','recoverable','user_action','info']，导致 34 项门禁中 2 项长期 FAIL；改为先锚定 `impl CrashCategory { }` 块再取 `CrashCategory::X => "id"`，并加 clean_exit 存在性断言。② 补齐「派生文案」这一最大空白：新增 rust/crates/rc-launcher-core/src/i18n/number.rs（区域感知数值格式化 —— format_int/format_uint/format_decimal/format_bytes/format_rate/format_byte_progress/format_percent/format_ratio_percent/format_fps/format_duration/format_duration_parts/format_relative_time/format_eta，及 mod.rs 中 bytes/rate/duration/eta/relative_time/percent/integer 当前语言简写），全部由目录键装配（新增 28 键 → 每语言 100→128 键）：format.{group_separator,decimal_separator,invalid_number,size,rate,percent,progress_of,duration_join,fps}、unit.{byte,kib,mib,gib,tib,pib}、duration.{zero,day,hour,minute,second}.{one,other}、relative.{now,past,future}、download.eta —— 因此单位名、分隔符与拼装模板均可翻译/可被 overlay 热更（示例：3 行 overlay 即把 en 改成法式「1,5 Kio」「1 234 567」）。③ 消除 UI 侧硬编码英文单位：ResourceMonitor.formatBytes 的 arrayOf("B","KB","MB","GB","TB") 与 "%.1f %s" 改为委托 RcValueFormat；ResourceSummary 的 "%.0f%% · %s / %s" 改走 percent+byteProgress；AwtScreen 的 String.format("%.1f fps") 改走 rcFps —— 中文用户不再看到中文句子包着英文单位。④ FFI/C ABI：ffi.rs 新增 i18n_format_json + i18n_format_kinds（11 种 kind，未知 kind 仍回数字并标 supported=false 以兼容版本偏斜；value 接受 JSON 数字或字符串以容纳 >2^53 的 Kotlin Long；负字节数钳到 0 而非回绕成 18 EB；digits 钳到 MAX_FRACTION_DIGITS），capi.rs 新增 rc_i18n_format，RustBridge.kt 新增 i18nFormat。⑤ Compose 侧 app/.../ui/i18n/RcValueFormat.kt（Rust 逐行手工移植 + RcStrings 扩展函数 + RcLocalization 的 rcBytes/rcRate/rcByteProgress/rcDuration/rcEta/rcRelativeTime/rcPercent/rcFps/rcInteger）——刻意用「本地装配」而非每标签一次 JNI：i18nBundle 已带回全部骨架键，故滚动下载列表零 FFI 穿越。⑥ 修复移植带来的两个真实缺陷：(a) 舍入不一致 —— Rust `{:.N}` 为 half-to-even（1.25→1.2）而 Java/Kotlin String.format 为 half-up（1.25→1.3），两侧对同一字节数会给出不同文本；Rust 侧显式实现 round_half_away_from_zero 并对齐 Java 规则（Kotlin 侧走 Locale.ROOT，避免 de-DE 设备注入逗号）；(b) 降级泄漏 —— RcStrings.plural 未命中时回显 key，空表（原生核缺失/引擎未初始化/@Preview）下 duration/eta/relative 会渲染成 「duration.minute.other duration.second.other」；DURATION_UNITS 改为三元组携带编译期兜底模板（{count} d/h/min/s）并新增 duration_piece/durationPiece，两侧同步。⑦ 语义修正：把「present-but-empty」的 format.group_separator 视为「该语言不分组」（group_separator/groupSeparator 专用查找），而 decimal_separator 仍兜底（空值会把 1 与 5 粘成 15）；zh 的 duration_join 由 {first}{second} 改为 {first} {second}，使中文读作「3 分 20 秒」而非挤在一起的「3 分20 秒」。⑧ 修好并加固门禁：gen_android_strings.py 对含字面 `%` 的值输出 formatted="false"（否则 aapt2 按 printf 说明符校验 {value}%）；check_i18n.py 由 34 项扩到 50 项，新增两移植体的静态一致性（字节单位阶梯同序、时长阶梯同序、兜底模板一致、读取键集一致、MAX_FRACTION_DIGITS 一致、空分组分隔符语义一致、小数分隔符仍兜底、i18nFormat kind 已在 RustBridge 文档化）与黄金基线新鲜度；且这些正则一律做空白容错，避免被 cargo fmt/ktlint 重排折断（与 ① 同类脆弱性）。⑨ 跨语言等价性机器证明：新增 examples/i18n_format_golden.rs 以 Rust 为 oracle 渲染 654 例矩阵（单位边界、舍入平局、0/负值、单位进位、单复数、3 语言）到 app/src/test/resources/i18n_format_golden.tsv（用 TSV 而非 JSON：测试零依赖、评审可逐行 diff），新增 Kotlin 单测 RcValueFormatParityTest 回放全部行并断言逐字节相同，CI build.yml 新增一步重跑生成器并 diff committed 基线。本轮已本地实测：cargo test --workspace 679 通过（较基线 654 新增 25，其中 i18n 相关 101 项）+ 3 doc test、cargo clippy --workspace --all-targets -D warnings 零警告、cargo fmt --check 通过、examples/i18n_demo 新增第 9 节（三语言数值/时长/相对时间 + overlay 换分隔符与单位）自断言通过、check_i18n.py 50/50 通过（128 键 × 3 语言）；并用独立 kotlinc 编译 RcValueFormat/RcStrings/AppLanguage 实跑 654 例黄金基线 + 降级用例，确认 PARITY OK，且以变异测试反证门禁有效（还原 half-to-even → 捕获 27 处不一致；删除单位进位守卫 → 捕获 18 处；篡改字节阶梯顺序/精度上限/兜底模板/移植键集/formatted="false"/黄金基线 → 均被对应门禁捕获）。；【本轮新增：支持动态语言加载】原先 Language 是编译期枚举、overlay 只能「改写」已内置语言，社区要加日语/俄语必须改 5 处文件重新出包（docs §6），对大陆用户尤其是不可接受的门槛。现新增 rust/crates/rc-launcher-core/src/i18n/pack.rs：把丢进 App 数据目录 i18n/ 的 .properties 注册为**一等语言**——① LanguagePack 解析 `_meta.{tag,native_name,english_name,plural,rtl,parent}` 头并把 `_meta.*` 从消息表剥离（永不泄漏为 UI key / all_keys）；② 注册表（BTreeMap + RwLock，容忍 poison）支持 install/install_text/load_dir/tags/with/contains/remove/clear/negotiate/set_active/lookup_active/describe；③ 新增 i18n::Scope{Builtin(Language), Pack(Arc<str>)} 作为唯一多出来的间接层，current_scope/current_language_tag/lookup_scoped/t_scoped/t_args_scoped/t_plural_scoped/bundle_scoped/bundle_for_tag/completeness_for_tag，t()/t_args()/t_plural() 与 bytes/rate/duration/eta/relative_time/percent/integer 全部改走 current_scope，故 pack 也能本地化字节单位与时长；number.rs 公开签名改为 impl Into<Scope>（From<Language> 保持所有既有调用点与 654 例黄金基线不变）；④ 复数规则从 Language 提取为 format::PluralRule{OtherOnly,OneOther}（Language::plural_rule + pack 的 `_meta.plural`），Scope::plural_rule 驱动 t_plural 与 duration_piece；⑤ available_languages/diagnostics 纳入 pack（dynamic/plural/parent/source/orphan_keys/placeholder_mismatch/pack_count/active_pack/current_tag）；⑥ set_language_tag 可选中 pack，set_language(内置) 主动取消 pack，set_language_from_preferences 逐条偏好「先内置后 pack」（["ja-JP","en"]→pack，["en","ja-JP"]→English）；⑦ 健壮性（任务 19 纪律）：读前先查 MAX_PACK_BYTES(1MiB)、MAX_PACKS(64) 上限且满额仍可更新既有 pack、与内置语言撞 tag 一律拒绝（那是 overlay 的职责，否则陈旧 en.properties 会以用户无法从选择器关掉的方式遮蔽英文）、零消息拒绝、und/mul/zxx 拒绝、未知 parent/plural 退回安全默认（回退链永远终止于中文、`.other` 永远可渲染）、卸载**正在使用**的 pack 自动回到其 parent（UI 不会指向已不存在的语言）、缺目录/非 UTF-8/非普通文件均为「跳过并记录原因」而非 panic，且每条原因经 skipped 返回给 UI（「我的翻译为什么不出现」本来是无法回答的支持问题）。FFI：ffi.rs 新增 requested_scope 与 i18n_language_packs_json（list/load/install/remove/clear），i18nTranslate/i18nBundle/i18nFormat 全部改走 Scope 并回报 dynamic/plural/rtl，i18nLanguages 增 current_tag/pack_count，i18nSetLanguage 增 tag_effective/dynamic；capi.rs 新增 rc_i18n_language_packs；RustBridge.kt 新增 i18nLanguagePacks。Compose 侧：新增 LanguageOption（枚举无法表示 pack，故选择器改由值对象驱动，builtin==null 即动态；partial 标记部分翻译；matches() 对齐核心 pack::negotiate）与 CoreLanguagePacks/RustBridgeLanguagePacks；RcStrings 增 tag/pluralRule（+ ofPack）使 pack 按**自己**的规则复数化，RcValueFormat.durationPiece 与 RcStrings.plural 改用之；新增 PackAwareStringsSource.loadTag（CoreStringsSource 实现、CompositeStringsSource 转发、ResourcesStringsSource 无法服务返回 null）；LocaleEngine 新增 languages/effectiveTag/packProblems 三个 StateFlow 与 loadLanguagePacks/removeLanguagePack/refreshLanguages/optionFor/applyTag/applyPack，init 时扫描 <filesDir>/i18n 并在「跟随系统」时也能命中 pack，核心不可用则选择器退回内置列表、已选 pack 退回其 parent（绝不空屏）。顺带修好一个既有真实缺陷：MiniJson 解析对象成员时逗号后未 skipWs，任何缩进/美化 JSON（数字后接换行是最常见形态）都会被拒绝——本次由动态语言 payload 暴露，已修复并补 MiniJsonTest 回归用例。门禁：check_i18n.py 由 47 项扩到 55 项，新增两侧复数规则 id 一致、`_meta.*` 读取键全部在模块文档中列出、`_meta.` 不与出货键撞、选择器消费核心实际输出的字段、内置撞 tag 守卫仍在（均做空白容错正则）。本轮已本地实测：cargo test --workspace 704 通过（较上轮 679 新增 25，i18n 相关 125 项）+ 3 doc、cargo clippy --workspace --all-targets -D warnings 零警告、cargo fmt --check 通过、examples/i18n_demo 新增第 10 节（安装 pack→进入选择器→翻译/速率/时长/回退中文→设备偏好协商→拒绝非法 pack→卸载回退）自断言通过、check_i18n.py 55/55、gen_android_strings.py --check 与 check_awt_wire.py 通过、黄金基线 diff 无变化；并用独立 kotlinc 实跑 654 例 Rust↔Kotlin 黄金基线仍 PARITY OK、新增 Kotlin 动态语言模型探针（核心真实 payload 解析、pack 复数化、matches、垃圾输入退回内置列表）全部通过；变异测试反证新门禁有效（删除内置撞 tag 守卫、引入未文档化 _meta 键 → 均被捕获）。]
21. [单元测试与集成测试] ——— [Rust 核心单元测试（下载/解析/校验/续传）与 Compose UI 测试（参考 FCL 与 MCTier 的 UI 测试模式，以 Robolectric 在 JVM 上运行），并在 CI 中强制通过门禁；项目仅面向 arm64-v8a 单一 ABI，故不引入需要 x86_64 模拟器的仪表化 / connected 测试。] [已完成 — Rust 核心单元/集成测试位于 rust/crates/rc-launcher-core/src/integration_tests.rs（下载/解析/校验/续传端到端集成测试 8 项，cargo test --workspace 全绿 530+2）；Compose UI 测试位于 app/src/test/java/com/rc/launcher/ui/component/InstanceCardTest.kt 与 app/src/test/java/com/rc/launcher/ui/navigation/RcAppSkeletonTest.kt 等（Robolectric JVM 单元测试，无需设备/模拟器，契合 arm64-v8a-only 策略）；CI 门禁：.github/workflows/build.yml 含 android-test（./gradlew testDebugUnitTest 作为强制门禁，Robolectric 在 JVM 上执行，不依赖任何 ABI 的模拟器）与 rust-test（cargo test --workspace 作为门禁，全绿 707 单元/集成 + 3 doc）。ABI 收敛：app/build.gradle.kts 与 core/build.gradle.kts 的 ndk.abiFilters 均为 arm64-v8a 单一目标，rust-core CI 矩阵仅 aarch64-linux-android，全局仅 arm64-v8a。] [本轮调整 — 移除模拟器（instrumented/connected）测试：此前曾尝试在 build.yml 新增 android-connected（reactivecircus/android-emulator-runner 拉起 API 34 x86_64 模拟器跑 connectedDebugAndroidTest），但该门禁需 x86_64 模拟器镜像，与「仅 arm64-v8a」的 ABI 收敛策略（任务 1 / docs/BUILD.md）冲突，故已整段删除；同时移除 app/src/androidTest 下的仪表化用例 ComposeNavigationTest / ExampleInstrumentedTest（其本质即跑在模拟器/设备上的「模拟器测试」，在 arm64-v8a-only 且无 arm64 模拟器 CI 的环境下无法稳定执行）。保留并夯实 Robolectric（JVM）安卓测试门禁 android-test，其不依赖任何设备 ABI，与单一 arm64-v8a 策略一致。Rust 侧 3 项新增端到端集成测试（mirror_fallback / resume_stale_meta / validate_sha1_md5）维持不变，cargo test --workspace 仍全绿 707 + 3 doc，cargo clippy --tests -D warnings 与 cargo fmt --check 均通过。]
22. [GitHub Actions：Rust 交叉编译与 Android 构建] —— [工作流使用 cargo-ndk 为各 ABI 编译 Rust 核心 .so，再 Gradle 构建并签名 APK/AAB；复刻 FCL build.yml 与 ZalithLauncher2 build.yml/push_ci 的流程，缓存 Cargo/Gradle 以加速。] [已完成 — 在 .github/workflows/build.yml 的 android job 中实现发布签名：从 KEYSTORE_BASE64 secret 解码 keystore 到 $rootDir/release-keystore.jks（仅当 secret 非空时执行，缺省则回退 debug 密钥），并把 KEYSTORE_PASSWORD/KEY_ALIAS/KEY_PASSWORD 经 env 注入 Gradle；app/build.gradle.kts 新增环境驱动的 release 签名配置（KEYSTORE_BASE64 存在时创建 ciRelease 签名配置并强校验 keystore/密码齐全，否则回退 debug 密钥），保证 fork PR 与本地 dry-run 仍可产出可安装 APK/AAB；复用 rust-core 的 cargo-ndk 四 ABI 交叉编译产物并组装到 core/src/main/jniLibs；新增 Verify APK signature 步骤用 apksigner 校验产物确已签名；Cargo 缓存新增 ~/.cargo/bin 以缓存 cargo-ndk 安装；README 补充签名所需 secrets 表与回退说明，复刻 FCL build.yml 与 Zalith push_ci 的 base64 keystore 在 runner 上还原流程，并对 Cargo/Gradle 做缓存加速。]
23. [GitHub Actions：自动化发布与产物管理] —— [自动打 tag、生成 Release、上传带 SHA-256 校验和的安装包、清理过期产物（cleanup-artifacts）；对标 FCL release.yml 与 Zalith release_ci。] [已完成 — 实现位于 .github/workflows/release.yml：自动打 tag（version job，push main/dispatch 自动生成 ci-<date>-<sha> 标签）、生成 Release（release job，softprops/action-gh-release + generate_release_notes + prerelease/make_latest）、SHA-256 校验和（dist/SHA256SUMS.txt，nullglob 安全处理 AAB 缺失）、清理过期产物（cleanup-artifacts job，gh api 分页删除超龄且保留最近 N 个 Artifact）；复用 build.yml 交叉编译并签名 APK/AAB]
24. [GitHub Actions：镜像源健康度与依赖审计] —— [定时任务测速各镜像源、校验清单哈希、扫描依赖许可证（借鉴 cuberite ThirdPartyLicenses 与 StyleCheck/FUNDING 工作流），输出健康度报告。] [已完成 — 实现位于 scripts/health_audit.py 与 .github/workflows/health.yml：定时任务（cron 每日 03:17 UTC + workflow_dispatch）测速 BMCLAPI/MCBBS/Aliyun 镜像源（可达性/响应时延/吞吐，对应 Rust MirrorProvider::measure）、校验 jre_manifest.json 的 SHA-1+size 并复用 generate_jre_manifest.py --check 闸门、扫描 cargo metadata 与 Gradle 依赖许可证并分桶（permissive/weak-copyleft/copyleft/incompatible/unknown，借鉴 cuberite ThirdPartyLicenses 与 StyleCheck/FUNDING 工作流），输出 health-report.json|md 与 third_party_licenses.md 健康度报告（写入 GITHUB_STEP_SUMMARY），并上传产物、异常时开/更新 issue。]
25. [性能与内存优化] —— [大文件流式下载/解压、协程调度避免主线程阻塞、对象池、渲染零拷贝；吸收 cuberite 高性能服务端与 MCTier 原生性能经验，保障低端机流畅。] [已完成 — 落地于 rust/crates/rc-launcher-core：① 新增 util::bufpool（BufPool 按 2 的幂分桶的可复用字节缓冲池 + 泛型 ObjectPool），消除热路径重复分配（借鉴 cuberite 的 bounded RegionCache 思路）；② 下载：HttpSource 新增 fetch_range_into 流式写出（ReqwestSource/NetworkClient 经 reqwest bytes_stream 增量落盘，分块不再整体驻留内存），元信息持久化改为「每 meta_persist_every 次限流 + spawn_blocking 序列化落盘」避免阻塞异步调度器，并新增 download_all_concurrent 有界并发批量下载；③ 解压：新增 extract_tar_xz_file / extract_tar_xz_reader，从磁盘流式 XZ 解压，避免把整个 .tar.xz 载入内存；④ 渲染零拷贝：AwtSession 内置帧缓冲对象池，copy_dirty_rgba_into_pooled / poll_frame_into_pooled 复用缓冲，每帧不再分配 Vec<u8>；⑤ Kotlin 侧本已通过 Dispatchers.IO 承载阻塞 JNI（AccountsScreen/MainViewModel/AccountRepository），与核心层 spawn_blocking 形成双层调度保护；⑥ 全部含单元测试，cargo test --workspace 全绿（538 + 2 doc）。]
26. [文档与贡献指南] —— [编写架构说明、模块接口、构建与发布文档（对应 FCL-Docs / Zalith zalithdocs），统一代码风格检查（如 cuberite StyleCheck），降低协作门槛。] [已完成 — 落地：docs/ARCHITECTURE.md（扩展为完整架构说明）、docs/MODULES.md（模块接口契约）、docs/BUILD.md（构建与发布，对应 FCL-Docs compilation.md）、CONTRIBUTING.md + docs/CONTRIBUTING.md（贡献指南，对应 FCL-Docs contribution.md）；统一代码风格检查：rustfmt.toml / clippy.toml / .editorconfig / config/detekt/detekt.yml，及 .github/workflows/stylecheck.yml（Rust 侧 cargo fmt --check + cargo clippy -D warnings 为严格门禁，Kotlin 侧 ktlintCheck + detekt 为 best-effort）；Rust 核心已清理至 clippy 零警告。]